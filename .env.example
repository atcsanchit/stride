# Public OAuth client ID (Web application). Safe to expose in the Vite bundle.
# Create it in Google Cloud → APIs & Services → Credentials.
# Authorized JavaScript origins:
#   http://localhost:5174
#   applink
VITE_GOOGLE_CLIENT_ID=
